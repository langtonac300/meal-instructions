Six-column category tile grid (photo with ink gradient + "n Meals" pill, uppercase name, mono heroTag); click filters the directory.

```jsx
<CategoryGrid categories={CATEGORIES} selected={cat} onSelect={setCat} counts={counts} />
```
