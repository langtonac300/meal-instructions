The floating bottom-right kitchen timer widget (mono countdown, accent progress bar, +1/+2/+5 MIN, START/PAUSE, chime when done).

```jsx
<KitchenTimer initialMinutes={12} label="Air Fryer Timer" onClose={close} />
<KitchenTimer fixed={false} initialMinutes={6} label="Flip mark" />
```
