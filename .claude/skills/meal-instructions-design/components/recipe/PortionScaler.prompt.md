Servings toggle (2 / 4 / 6 / 8★) for scaling ingredient quantities.

```jsx
<PortionScaler currentServings={4} onChange={setServings} />
<PortionScaler variant="multiplier" currentServings={4} onChange={setServings} />
```
