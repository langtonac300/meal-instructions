The ⌘K search dialog — blurred ink scrim, mono input, quick tags, result rows with #ID / GUIDE / STORAGE badges, key-hint footer.

```jsx
<SearchModal isOpen={open} onClose={() => setOpen(false)} items={searchItems} onSelect={(it) => go(it.href)} />
```
