Site header: ink ticker bar + sticky blurred paper header with logo, 10 mono uppercase links, Search ⌘K trigger and Sign in.

```jsx
<Navbar activeHref="/how-long" onNavigate={go} onSearch={() => setSearchOpen(true)} />
```
